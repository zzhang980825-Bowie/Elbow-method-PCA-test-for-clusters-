from sklearn.cluster import KMeans
import matplotlib.pyplot as plt
import numpy as np

inertia_values = []

for i in range(1, 6):
    kmeans = KMeans(n_clusters=i)
    kmeans.fit(df_re_1.iloc[:,1:])
    inertia_values.append(kmeans.inertia_)

plt.plot(range(1, 6), inertia_values, marker='o')
plt.xlabel('Number of Clusters')
plt.ylabel('Inertia')
plt.title('Elbow Method for Optimal Clusters')
plt.show()

kmeans_1 = KMeans(n_clusters=2)
kmeans_1.fit(df_re_1.iloc[:,1:])
df_re_1['Cluster'] = kmeans_1.labels_
df_re_1

silhouette_avg_1 = silhouette_score(df_re_1.iloc[:,1:], kmeans_1.labels_)
print('silhouette score：',silhouette_avg_1)

inertia_values = []

for i in range(1, 6):
    kmeans = KMeans(n_clusters=i)
    kmeans.fit(df_re_2.iloc[:,1:])
    inertia_values.append(kmeans.inertia_)

plt.plot(range(1, 6), inertia_values, marker='o')
plt.xlabel('Number of Clusters')
plt.ylabel('Inertia')
plt.title('Elbow Method for Optimal Clusters')
plt.show()

kmeans_2 = KMeans(n_clusters=2)
kmeans_2.fit(df_re_2.iloc[:,1:])
df_re_2['Cluster'] = kmeans_2.labels_
df_re_2

silhouette_avg_2 = silhouette_score(df_re_2.iloc[:,1:], kmeans_2.labels_)
print('silhouette score：',silhouette_avg_2)

inertia_values = []

for i in range(1, 6):
    kmeans = KMeans(n_clusters=i)
    kmeans.fit(df_re_3.iloc[:,1:])
    inertia_values.append(kmeans.inertia_)

plt.plot(range(1, 6), inertia_values, marker='o')
plt.xlabel('Number of Clusters')
plt.ylabel('Inertia')
plt.title('Elbow Method for Optimal Clusters')
plt.show()

kmeans = KMeans(n_clusters=2)
kmeans.fit(df_re_3.iloc[:,1:])
df_re_3['Cluster'] = kmeans.labels_
df_re_3

silhouette_avg_3 = silhouette_score(df_re_3.iloc[:,1:], kmeans.labels_)
print('silhouette score：',silhouette_avg_3)

